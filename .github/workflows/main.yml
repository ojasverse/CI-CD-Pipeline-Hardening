name: CI Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run App
        run: node app.js

      - name: Login (BAD PRACTICE)
        run: echo "Password is mysecret123"
